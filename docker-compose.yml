services:
  translet:
    build: .
    image: elcamilet/translet:latest
    ports:
      - "3000:3000"
    environment:
      - DEEPL_API_KEY=${DEEPL_API_KEY}
      - PORT=${PORT}
    restart: unless-stopped
